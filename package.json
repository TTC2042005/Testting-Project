{
  "name": "jira-config-runner",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node run.js",
    "check": "node run.js --check",
    "comment": "node run.js",
    "jira:check": "npm run check",
    "jira:comment": "npm run comment",
    "jira:validate-credentials": "node run.js --check",
    "test": "jest",
    "test:config": "jest __tests__/jira.config.test.js",
    "test:integration": "jest __tests__/integration",
    "test:system": "jest __tests__/system",
    "test:auto": "npm run test:config && npm run test:integration && npm run test:system",
    "test:record": "node write-test-results.js",
    "test:report": "node -e \"const { postFailureToJira, resolveIssueKey } = require('./jira-report'); const args = process.argv.slice(1); const issue = resolveIssueKey(args); postFailureToJira({ issueKey: issue, command: 'manual', exitCode: 1, output: 'Manual report' }).then((result) => console.log(JSON.stringify(result)))\""
  },
  "devDependencies": {
    "jest": "^30.4.2"
  }
}
